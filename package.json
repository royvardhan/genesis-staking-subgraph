{
  "name": "GenesisStaking",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ GenesisStaking",
    "create-local": "graph create --node http://localhost:8020/ GenesisStaking",
    "remove-local": "graph remove --node http://localhost:8020/ GenesisStaking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 GenesisStaking",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "matchstick-as": "0.5.0"
  }
}
